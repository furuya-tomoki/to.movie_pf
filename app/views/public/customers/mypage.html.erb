


<div class="container">
  <div class="row mt-2">
   <div class="col-md-3 mb-4 text-center">
     <h4 style="background:#EEEEEE">いいね一覧</h4>
   </div>
   </div>


<table class="table">
  <thead>
    <tr bgcolor="#EEEEEE">
      <th>タイトル</th>
    </tr>
  </thead>

  <% @favorites.each do |favorite| %>
    <tbody>
    <tr>
      <td>
        <%= link_to favorite.movie.title, movie_movie_comments_path(favorite.movie) %></td>
  </tbody>
  <% end %>
</table>
</div>